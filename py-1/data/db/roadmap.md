# Dev-mode roadmap

Progress against the capability checklist in the dev `systemPrompt` (the target
the agent grows into). The agent updates this file as it ships features. The
checklist itself is operator-owned and immutable; this is the working tracker.

| # | Capability | Status | Comments |
|---|------------|--------|-------|
| 1 | Context compaction beyond the safety clip | not started | |
| 2 | web_fetch / web_search result caching | not started | |
| 3 | Richer /data/db memory index (retrieval, provenance) | not started | |
| 4 | Robust provider + backend adapters (retries, rate limits) | partial | basic retry/backoff shipped |
| 5 | Growing /src/tests suite (behaviour, not just liveness) | partial | seed suite present |
| 6 | Cost/latency telemetry beyond the budget counter | not started | |
